<h1><%= @task.title %></h1>
<h3>Located in <%= @task.details %></h3>

<p><%= link_to 'Back to index', task_path %></p>
<p><%= link_to 'Edit this task', task_path(@task) %></p>
<p><%= link_to 'Delete',
      task_path(@task),
       data: { turbo_method: :delete, turbo_confirm: 'You sure?' } %></p>
